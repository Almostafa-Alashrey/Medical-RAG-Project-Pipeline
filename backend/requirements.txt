flask==3.0.3
flask-cors==4.0.1
numpy==1.26.4

# --- Optional, heavier dependencies used by the "full" medical RAG pipeline ---
# The backend will automatically fall back to a lightweight lexical/TF-IDF style
# retrieval + extractive answer mode if these are not installed, so the API
# always runs. Install them for the full experience described in README.md:
#
# sentence-transformers==3.0.1
# faiss-cpu==1.8.0
# torch==2.3.1
# transformers==4.44.2